{
  "name": "evanhahn.com",
  "private": true,
  "devDependencies": {
    "prettier": "^2.8.3"
  },
  "scripts": {
    "format": "prettier --write .",
    "lint": "prettier --check .",
    "test": "npm run lint"
  }
}
